/**
 * Placeholder for chrome that still lives in the static HTML shell
 * (navbar, tabs, sidebar, toolbar, modals). Phases 3–8 will progressively
 * pull those into React; for Phase 2 the legacy DOM continues to render
 * itself and this component intentionally emits nothing.
 */
export const LegacyShell = (): null => null;
