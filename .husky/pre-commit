#!/usr/bin/env sh
# Pre-commit gate: formatting, lint, and unit tests must all pass
# before a commit lands locally. CI runs the same checks plus the
# build verification (see .github/workflows/tests.yml).

npm run prettier-check && npm run lint && npm test
